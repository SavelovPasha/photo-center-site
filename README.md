# ФотоЦентр

Статический сайт фотоцентра.

## Локальный запуск

- `open-local-site.bat` - запускает локальный сервер и открывает сайт в браузере
- `start-site.bat` - запускает только локальный сервер

После запуска сайт доступен по адресу:

`http://localhost:8000`
